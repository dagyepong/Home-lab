# Move the script to a global bin
```bash
sudo cp install.sh /usr/local/bin/h-install

# Make it executable
sudo chmod +x /usr/local/bin/h-install
```


Navigate: cd ~/homelab/services/vaultwarden/

Execute: h-install