# Immich version – 'release' tracks the latest stable
IMMICH_VERSION=release

# Storage paths
UPLOAD_LOCATION=/media/dfr               # Absolute path for uploaded photos/videos
DB_DATA_LOCATION=./postgres              # Relative path for PostgreSQL data

# Database credentials – CHANGE THESE!
DB_PASSWORD=your_strong_password_here    # ← replace with a secure password
DB_USERNAME=postgres
DB_DATABASE_NAME=immich

# Redis host (internal Docker service name)
REDIS_HOSTNAME=immich_redis

# Force binding to all interfaces (fixes 502)
HOST=0.0.0.0
IMMICH_HOST=0.0.0.0
IMMICH_SERVER_HOST=0.0.0.0
IMMICH_SERVER__HOST=0.0.0.0
NESTJS_HOST=0.0.0.0


## Pocketid
# ========== POCKET ID ==========
PUBLIC_APP_URL=https://pocketid.linuxpad.blog   # ← change to your Pocket ID domain
TRUST_PROXY=true

# ========== IMMICH OIDC with Pocket ID ==========
POCKET_ID_ISSUER=https://pocketid.linuxpad.blog
POCKET_ID_CLIENT_ID=<client-id-from-pocket-id>      # to be filled after OIDC client creation
POCKET_ID_CLIENT_SECRET=<client-secret-from-pocket-id>